export const signup = async (req, res) => {
  res.json({
    data: "Signup endpoint has been reached",
  });
};

export const login = async (req, res) => {
  res.json({
    data: "Login endpoint has been reached",
  });
};

export const logout = async (req, res) => {
  res.json({
    data: "Logout endpoint has been reached",
  });
};
